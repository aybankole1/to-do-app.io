# to-do-app.io
A clean and responsive To-Do List App that helps users organize tasks efficiently. It supports adding, editing, deleting, marking tasks as completed, and stores all data using localStorage so tasks remain saved even after refreshing the page.
